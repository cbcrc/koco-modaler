{
  "name": "koco-modaler",
  "version": "1.0.0",
  "authors": [
    "Frédérik Charest & Maxime Séguin"
  ],
  "description": "",
  "main": "src/modaler.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "dependencies": {
    "jquery": "^2.1.3",
    "bootstrap": "^3.3.2",
    "knockout": "^3.3.0",
    "lodash": "^3.0.0",
    "requirejs": "^2.1.15",
    "requirejs-text": "^2.0.13",
    "koco-knockout-utilities": "^0.4.0"
  }
}
